using System;
using parse.Models;

namespace parse
{
    public class Parser
    {
        public ConfigFile ParseConfigFile(string filePath)
        {
            var result = new ConfigFile();
            return result;
        }
    }
}
